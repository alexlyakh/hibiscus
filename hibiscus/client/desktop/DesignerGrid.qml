import QtQuick 2.0

Grid {
    id: designerGrid
    anchors.fill: parent
    anchors.leftMargin: 00
    rows: 3
    columns: 12
    spacing: 20

    Column {
        id: column1

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column2

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column3

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column4

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column5

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column6

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column7

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column8

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column9

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column10

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column11

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }

    Column {
        id: column12

        Rectangle {
            color: "beige"
            height: designerGrid.height
            width: 60
        }
    }
}
